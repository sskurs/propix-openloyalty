import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function CommunicationPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Communication Management</CardTitle>
        <p className="text-sm text-muted-foreground">This page will be used to manage email, SMS, and push notification templates and triggers.</p>
      </CardHeader>
      <CardContent>
        <p>Functionality for this page will be implemented in a future step.</p>
      </CardContent>
    </Card>
  );
}
