import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function ImportExportPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Member Import / Export</CardTitle>
        <p className="text-sm text-muted-foreground">This page will show the logs of all member import and export jobs.</p>
      </CardHeader>
      <CardContent>
        <p>Functionality for this page will be implemented in a future step.</p>
      </CardContent>
    </Card>
  );
}
